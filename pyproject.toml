[project]
name = "modelnet10-classifier-dl"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "open3d",
    "numpy",
    "torch",
    "torchvision",
    "tensorboard",
    "tqdm"
]
